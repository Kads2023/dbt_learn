versoion: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    freshness: 
      error_after:
        count: 1
        period: day
      warn_after:
        count: 24
        period: hour
    loaded_at_field: _ETL_LOADED_AT
    
    tables:
      - name: customers
        freshness: null
      - name: orders
  - name: stripe
    database: raw
    schema: stripe
    freshness: 
      error_after:
        count: 1
        period: day
      warn_after:
        count: 24
        period: hour
    loaded_at_field: _batched_at
    tables:
      - name: payment